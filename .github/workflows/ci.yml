name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test-and-render:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Run tests
        run: go test ./...

      - name: Build erdn
        run: go build -o erdn ./cmd/erdn

      - name: Install Graphviz
        run: sudo apt-get update -qq && sudo apt-get install -y graphviz

      - name: Render examples to PNG
        run: |
          for f in examples/*.erdn; do
            out="${f%.erdn}.png"
            ./erdn render "$f" --format png --out "$out"
            echo "rendered: $out"
          done

      - name: Upload PNG diagrams
        id: upload
        uses: actions/upload-artifact@v4
        with:
          name: er-diagrams
          path: examples/*.png
          if-no-files-found: error

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        env:
          ARTIFACT_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        with:
          script: |
            const fs = require('fs');
            const path = require('path');

            // Collect rendered PNGs and their DOT sources
            const examplesDir = 'examples';
            const erdn = fs.readdirSync(examplesDir).filter(f => f.endsWith('.erdn'));

            let sections = '';
            for (const file of erdn) {
              const name = path.basename(file, '.erdn');
              const pngPath = path.join(examplesDir, `${name}.png`);
              const pngExists = fs.existsSync(pngPath);

              // Read DOT source via CLI output (stored earlier)
              sections += `\n### \`${file}\`\n\n`;
              if (pngExists) {
                const pngSize = fs.statSync(pngPath).size;
                sections += `PNG rendered successfully (${(pngSize / 1024).toFixed(1)} KB)\n\n`;
              }
            }

            const artifactUrl = process.env.ARTIFACT_URL;

            const body = [
              '## 🗂 ER Diagram Render',
              '',
              '✅ **Tests passed** — `go test ./...`',
              '',
              sections.trim(),
              '',
              '---',
              `📦 **[Download PNG diagrams](${artifactUrl})** — open the workflow run and expand *Artifacts* to download \`er-diagrams.zip\`.`,
            ].join('\n');

            // Find existing bot comment and update it, or create a new one
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(c =>
              c.user.type === 'Bot' && c.body.includes('ER Diagram Render')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }
